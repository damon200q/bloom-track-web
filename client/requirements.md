## Packages
date-fns | Date calculations and formatting
framer-motion | Smooth animations for result reveals and page transitions
react-day-picker | Calendar component for date selection (often used with shadcn/ui)

## Notes
The fertility calculation logic (ovulation = next period - 14 days) is implemented on the frontend for immediate feedback, but also saved to the backend.
API endpoints defined in shared/routes.ts are used for persistent history.
Theme colors: Primary Pink #ff6b81, Background #fff5f6.
